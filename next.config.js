/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  images: {
    domains: ['res.cloudinary.com'], // Replace 'example.com' with the domain(s) from which you'll be loading images
  },
};
